services:
  db:
    image: postgres:17
    container_name: movie_db
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backend/sql/createDatabase_V2.sql:/docker-entrypoint-initdb.d/createDatabase_V2.sql

  backend:
    build: ./backend
    container_name: movie_backend
    env_file:
      - ./backend/.env
    environment:
      DB_HOST: db
    depends_on:
      - db


  frontend:
    build: 
      context: ./frontend
      args:
        VITE_API_URL: /api
    container_name: movie_frontend
    ports:
      - "${FRONTEND_PORT}:${FRONTEND_PORT}"
    depends_on:
      - backend

volumes:
  postgres_data: